package com.practice;

/*
 * Given two integers (positive , negative or even 0) say a and b.
 *  Find the minimum value of a$b where $ is any arithmetic operation like multiply(*), Divide(/), Addition(+),Substraction(-). 
 *  Use Exception Handling in this problem.
 */
public class Demo {
	
	// Define private variables, Constructors to initialize 
    // variables with the given values and a method findMin() which prints the 
    // minimum value of a$b.	
	
}
